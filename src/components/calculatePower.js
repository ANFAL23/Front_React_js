
const calculatePower = (pokemon) => {
    return pokemon.speed + pokemon.hp + pokemon.attack + pokemon.defense + pokemon.special_attack + pokemon.special_defense;
  };
export default calculatePower;  